# SingleSystem action backbone (cfg.model.action_backbone.*): action tokens share
# the video DiT sequence; the variant (vanilla | moe) is picked in single_system.yaml.
action_decoder_hidden_dim: 1024   # shared action decoder MLP width
expert_ffn_dim: 4096              # moe only: expert FFN intermediate width
